# Copy to .env and fill values
DATABASE_URL=
# Set to true for hosted Postgres that requires SSL (e.g., Render). False for local docker-compose Postgres.
DATABASE_SSL=false
# Single token used for both read and write endpoints
MCP_TOKEN=
OPENAI_API_KEY=
